# `for_each` Manifests

These examples demonstrate stable keyed resource creation.

```bash
terraform fmt -recursive
terraform init
terraform validate
terraform plan
```

Prefer stable keys and inspect address changes before applying updates to existing resources.