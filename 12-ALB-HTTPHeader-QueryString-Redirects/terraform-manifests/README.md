# ALB Advanced Rule Manifests

These manifests demonstrate HTTP header, query-string and redirect conditions/actions.

```bash
terraform fmt -recursive
terraform init
terraform validate
terraform plan
```

Treat routing conditions as routing logic, not authentication. Test both matching and fallback behavior.