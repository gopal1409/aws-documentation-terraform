# Terraform Language Syntax — Manifests

The `.tf` files in this directory demonstrate Terraform/HCL blocks and expressions.

Validate examples with:

```bash
terraform fmt -recursive
terraform init
terraform validate
```

Examples are educational. Replace account-specific values and never add real credentials or secrets.