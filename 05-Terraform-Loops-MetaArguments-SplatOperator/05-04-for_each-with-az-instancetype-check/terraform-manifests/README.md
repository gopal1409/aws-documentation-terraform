# `for_each` Validation — Manifests

These files demonstrate keyed resources plus AWS/region validation logic.

```bash
terraform fmt -recursive
terraform init
terraform validate
terraform plan
```

Keep region, Availability Zone and instance-type assumptions configurable and document supported combinations.