# `count` and `for` Manifests

Examples here demonstrate resource expansion and collection expressions.

```bash
terraform fmt -recursive
terraform init
terraform validate
terraform plan
```

Review resource addresses carefully when changing `count` inputs. Keep secrets out of source control.